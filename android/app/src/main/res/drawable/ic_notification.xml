<!-- Icône de notification ARENA (status bar). Silhouette blanche sur
     transparent — Android force le monochrome pour la petite icône, donc
     PAS d'icône couleur (sinon carré blanc). Reprend le chevron du logo
     ARENA. La teinte est appliquée via meta-data default_notification_color. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M6,4 L18,12 L6,20 L6,15.5 L12.5,12 L6,8.5 Z" />
</vector>
