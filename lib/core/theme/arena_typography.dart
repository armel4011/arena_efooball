// ─────────────────────────────────────────────────────────────────────────
// LEGACY SHIM — `ArenaTypography` lives in `arena_theme.dart` (v2). Existing
// imports keep working through this re-export until each wave migrates its
// screens onto v2 token names (ArenaText.h1/body/mono/…).
// Removed at the end of wave 4.
// ─────────────────────────────────────────────────────────────────────────
export 'package:arena/core/theme/arena_theme.dart' show ArenaTypography;
